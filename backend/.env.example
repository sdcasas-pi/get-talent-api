APP_NAME=student-registration-service
DEBUG=false
# Opcional: si no se define, por defecto usa <repo>/data/db/students.db
# (data/ al mismo nivel que backend/ y frontend/). El directorio se crea solo si no existe.
# DATABASE_URL=sqlite+aiosqlite:///../data/db/students.db
